import 'package:flutter_test/flutter_test.dart';
import 'package:family_rewards/main.dart';

void main() {
  testWidgets('App class exists', (WidgetTester tester) async {
    expect(FamilyRewardsApp, isNotNull);
  });
}
